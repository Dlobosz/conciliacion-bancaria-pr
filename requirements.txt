# Dependencias de ejecucion (las que necesita la app en produccion).
# Las de desarrollo estan en requirements-dev.txt

# Manipulacion de datos
pandas==2.2.3
openpyxl==3.1.5

# Fuzzy matching (MIT, implementado en C++)
rapidfuzz==3.10.1

# Dashboard
streamlit==1.40.2

# Cliente LLM (solo para excepciones)
anthropic==1.1.0
python-dotenv==1.0.1
